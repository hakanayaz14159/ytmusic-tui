[project]
name = "ytmusic-player-cli"
dynamic = ["version"]
description = "Unofficial terminal music player that streams YouTube audio. Not affiliated with Google or YouTube."
authors = [
    {name = "Hakan Ayaz", email = "hakanayaz14159@gmail.com"}
]
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.11"
keywords = ["youtube", "music", "cli", "tui", "terminal"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: End Users/Desktop",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Multimedia :: Sound/Audio",
    "Topic :: Terminals",
]

dependencies = [
    "textual>=8.0.0",
    "yt-dlp>=2025.0.0",
    "python-vlc>=3.0.21",
    "peewee>=3.18.0",
    "click>=8.1.0",
    "platformdirs>=4.3.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
    "pytest-cov>=6.0.0",
    "pytest-asyncio>=0.24.0",
    "pytest-mock>=3.14.0",
    "textual-dev>=1.8.0",
    "ruff>=0.9.0",
    "mypy>=1.14.0",
    "pre-commit>=4.0.0",
]

[project.scripts]
ytmusic-tui = "ytmusic_tui.main:main"

[project.urls]
Homepage = "https://github.com/hakanayaz14159/ytmusic-tui"
Repository = "https://github.com/hakanayaz14159/ytmusic-tui.git"
Issues = "https://github.com/hakanayaz14159/ytmusic-tui/issues"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.version]
path = "ytmusic_tui/__init__.py"

[tool.hatch.build.targets.wheel]
packages = ["ytmusic_tui"]

[tool.hatch.build.targets.sdist]
include = ["/ytmusic_tui", "/README.md", "/LICENSE"]

[tool.ruff]
target-version = "py311"
line-length = 88
extend-exclude = [
    ".git",
    ".mypy_cache",
    ".ruff_cache",
    ".venv",
    "__pycache__",
    "build",
    "dist",
]

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # pyflakes
    "I",   # isort
    "B",   # flake8-bugbear
    "C4",  # flake8-comprehensions
    "UP",  # pyupgrade
    "ARG", # flake8-unused-arguments
    "SIM", # flake8-simplify
    "TCH", # flake8-type-checking
    "PTH", # flake8-use-pathlib
    "ERA", # eradicate
    "PL",  # pylint
    "RUF", # ruff-specific rules
]
ignore = [
    "E501",   # line too long, handled by formatter
    "PLR0913", # too many arguments to function call
    "PLR0912", # too many branches
    "PLR0915", # too many statements
]

[tool.ruff.lint.per-file-ignores]
"tests/**/*" = ["PLR2004", "S101", "TID252", "ARG001", "ARG002", "TC001", "TC002"]
"ytmusic_tui/**/*" = []

[tool.ruff.format]
quote-style = "double"
indent-style = "space"

[tool.mypy]
python_version = "3.11"
check_untyped_defs = true
disallow_any_generics = true
disallow_incomplete_defs = true
disallow_untyped_defs = true
no_implicit_optional = true
warn_redundant_casts = true
warn_unused_ignores = true
warn_unreachable = true
strict_equality = true
warn_return_any = true
disallow_untyped_calls = true
disallow_subclassing_any = true
warn_unused_configs = true

[[tool.mypy.overrides]]
module = [
    "peewee.*",
    "yt_dlp.*",
    "vlc.*",
]
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = "ytmusic_tui.db.db"
disallow_subclassing_any = false

[tool.pytest.ini_options]
minversion = "8.0"
addopts = "-ra -q --strict-markers --strict-config -m \"not network\""
testpaths = ["tests"]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
markers = ["network: hits real YouTube via yt-dlp; skipped when offline"]
filterwarnings = [
    "error",
    "ignore::UserWarning",
    "ignore::DeprecationWarning",
]

[tool.coverage.run]
source = ["ytmusic_tui"]
branch = true

[tool.coverage.report]
fail_under = 83
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "if self.debug:",
    "if settings.DEBUG",
    "raise AssertionError",
    "raise NotImplementedError",
    "if 0:",
    "if __name__ == .__main__.:",
    "class .*\\bProtocol\\):",
    "@(abc\\.)?abstractmethod",
]
